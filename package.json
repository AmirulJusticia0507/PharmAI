{
    "name": "pharmai",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "concurrently -n api,web -c yellow,cyan \"python -m uvicorn app.main:app --reload --port 8000 --app-dir backend\" \"npm run dev --prefix frontend\"",
        "build": "npm --prefix frontend install --include=dev && npm --prefix frontend run build"
    },
    "dependencies": {
        "next": "^15.5.25",
        "react": "^19.0.0",
        "react-dom": "^19.0.0"
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}
